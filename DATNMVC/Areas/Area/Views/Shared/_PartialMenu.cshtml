@model IEnumerable<DATNMVC.Models.Khoa>
<div class="panel panel-default">
    <div class="panel-heading">
        @foreach (var item in Model)
        {
            <h4 class="panel-title" style="
        background: #008dc2;
        margin-top: 2px;
        font-weight: bold;
        color: #fefefe;
        min-height: 35px;
        line-height: 34px;
        padding-left:5px
">
                @Html.ActionLink(@item.Name, "GetThongTinGiangVien", new { id = item.Id }, new { id = "@Id" })
                <span data-id="15" class="morebtn" style=" float: right;
        margin-right: 5px;
        font-size: 16px;
        width: 28px;
        height: 28px;
        line-height: 28px;
        border-radius: 28px;
        background-color: #3554c5;
        text-align: center;
        margin-top: 3px;
    ">+</span>
            </h4>
        }
    </div>
</div>